// App configuration.
module.exports = {
  // The session locks after this many minutes of inactivity.
  SESSION_TIMEOUT_MINUTES: 15,
  // The maximum number of notes a user can store.
  MAX_NOTES: 100,
};
